Django==5.0.6              # core framework
djangorestframework==3.15.2  # for building REST APIs
psycopg2-binary==2.9.9     # PostgreSQL adapter (if using Postgres)
python-decouple==3.8       # manage environment variables (API keys, secrets)